# Frontend environment variables (Vite)
# Copy this file to `.env` and adjust values per environment.

# Backend API base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:8000

# App display name
VITE_APP_NAME=POS Boutique

