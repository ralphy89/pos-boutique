# Example for production build (copy values into your CI env or a local file not committed to git)
# Vite embeds VITE_* into the bundle — use only public URLs here.

VITE_API_BASE_URL=https://api.example.com
VITE_APP_NAME=POS Boutique
